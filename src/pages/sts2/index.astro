---
import Layout from '../../components/Layout.astro';
import Badge from '../../components/Badge.astro';
import { getData } from '../../lib/data';
import { getLocale } from '../../lib/i18n';

const { locale, langPrefix } = getLocale(Astro);
const data = await getData('sts2', locale);
const counts = {
  cards: data.cards.length,
  relics: data.relics.length,
  potions: data.potions.length,
  monsters: data.monsters.length,
  events: data.events.length,
  powers: data.powers.length,
  keywords: data.keywords.length,
  enchantments: data.enchantments.length,
};

const taglineMap: Record<string, string> = {
  IRONCLAD: 'Strength & self-healing',
  SILENT: 'Poison & card draw',
  DEFECT: 'Orbs & channeling',
  NECROBINDER: 'Summons & sacrifice',
  REGENT: 'Stars & burst turns',
};
const charColors: Record<string, string> = {
  IRONCLAD: 'border-red-500/20 hover:border-red-500/40',
  SILENT: 'border-green-500/20 hover:border-green-500/40',
  DEFECT: 'border-blue-500/20 hover:border-blue-500/40',
  NECROBINDER: 'border-purple-500/20 hover:border-purple-500/40',
  REGENT: 'border-amber-500/20 hover:border-amber-500/40',
};

const browseLinks = [
  { href: langPrefix + '/sts2/cards', icon: 'cards', label: 'Cards', desc: counts.cards + ' cards across all characters' },
  { href: langPrefix + '/sts2/relics', icon: 'relics', label: 'Relics', desc: counts.relics + ' relics' },
  { href: langPrefix + '/sts2/potions', icon: 'potions', label: 'Potions', desc: counts.potions + ' potions' },
  { href: langPrefix + '/sts2/monsters', icon: 'monsters', label: 'Monsters', desc: counts.monsters + ' monsters' },
  { href: langPrefix + '/sts2/events', icon: 'events', label: 'Events', desc: counts.events + ' events' },
  { href: langPrefix + '/sts2/effects', icon: 'effects', label: 'Buffs & Debuffs', desc: counts.powers + ' buffs & debuffs' },
  { href: langPrefix + '/sts2/enchantments', icon: 'enchantments', label: 'Enchantments', desc: counts.enchantments + ' card enchantments' },
  { href: langPrefix + '/sts2/keywords', icon: 'keywords', label: 'Keywords', desc: counts.keywords + ' keywords' },
];
---
<Layout game="sts2" title="Slay the Spire 2 — Spire Archive" description="The complete database for Slay the Spire 2 — browse cards, relics, potions, characters, monsters, events, and more.">
  <h1 class="text-2xl font-bold">Slay the Spire 2</h1>
  <p class="mt-2 max-w-2xl text-sm text-slate-400">
    The complete database for Slay the Spire 2
  </p>

  <section class="mt-6">
    <h2 class="text-xs font-semibold text-amber-200/60 uppercase tracking-widest">Characters</h2>
    <div class="mt-3 grid grid-cols-2 gap-3 md:grid-cols-5">
      {data.characters.map((ch) => (
        <a href={langPrefix + '/sts2/characters/' + ch.id} class={'group rounded-lg border bg-white/[0.03] overflow-hidden hover:bg-white/[0.06] text-center transition-all ' + (charColors[ch.id] ?? 'border-white/10')}>
          <img src={'/images/sts2/characters/' + ch.id.toLowerCase() + '.jpg'} alt={ch.name} class="w-full h-24 object-cover opacity-80 group-hover:opacity-100 transition-opacity" style="object-position: right center" />
          <div class="p-3">
            <div class="text-sm font-semibold">{ch.name}</div>
            <div class="mt-1 text-xs text-slate-500">{taglineMap[ch.id] ?? ''}</div>
          </div>
        </a>
      ))}
    </div>
  </section>

  <section class="mt-8">
    <h2 class="text-xs font-semibold text-amber-200/60 uppercase tracking-widest">Browse</h2>
    <div class="mt-3 grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-3">
      {browseLinks.map((item) => (
        <a href={item.href} class="group flex items-center gap-3 rounded-lg border border-white/[0.06] bg-white/[0.03] p-4 hover:bg-white/[0.06] hover:border-white/[0.12] transition-all">
          <img src={'/images/sts2/nav/' + item.icon + '.png'} alt="" class="w-8 h-8 object-contain opacity-60 group-hover:opacity-100 transition-opacity" />
          <div>
            <div class="text-sm font-medium">{item.label}</div>
            <div class="mt-1 text-xs text-slate-500">{item.desc}</div>
          </div>
        </a>
      ))}
    </div>
  </section>
</Layout>
